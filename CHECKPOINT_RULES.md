# Checkpoint Rules

0. 所有回答都必须清晰、准确、诚实：区分已核实事实、基于证据的推断和无法确认；不得编造、夸大或遗漏会改变用户判断的关键事实；答错后必须立即明确更正。
1. 看到 `AWAITING USER INPUT`、`main_state.json.status=awaiting_*`，或 `run_step.py` 返回退出码 `4` 时，必须立即停止。
2. 停止后只能做四件事：读 `main_state.json`、读 `interaction.json`、把交互内容整理成用户可读的决策卡片、等待用户回复。
3. 禁止跳过用户确认，禁止替用户选择 `continue`，禁止伪造用户答复。
4. 恢复时优先使用 `--response-json` 或 `--response-file`，把用户真实答复整理成 `intent_patch` 后传回 `run_step.py`。
5. 在用户未回复前，禁止执行任何“继续”“恢复”“下一步”命令。
6. 决策卡片必须覆盖当前所有交互点中确有必要的事项：缺少 Step1 输入、Step1/Step2 的真实业务选择、Step4 至少两个候选依赖时的 Step5 范围选择、需要用户授权的环境前提，以及从指定步骤重跑；0/1 个 Step4 候选和可内部恢复的网络、ref、超时或工具故障不得制造确认点。
7. 给用户看的第一层只写决策信息：当前要确认什么、为什么停下、可选动作、需要补什么、候选对象、完整候选或证据文件、可以直接怎么回复。
8. `response_schema`、`action_requirements`、`selection_resolution`、`input_normalization`、`runtime_rules` 只用于整理恢复命令，不作为用户主信息展示。
9. 回答任务是否完成前，必须重新核对任务清单、当前验证、Git 状态和用户仍然有效的要求。每项执行状态必须按事实写成“已完成”“正在执行”“待执行”“阻塞”或“失败”；“正在执行”仅限报告时确有实际工作正在运行，尚未开始必须写“待执行”。
10. 验证状态单独写成“已验证”或“未验证”。多项任务必须报告总数、各状态数量和未完成项；“未完成”只作总体结论，不能代替具体状态。
11. 局部实现、单元测试或单个真实项目通过不能表述为全部完成；只要仍有待执行、正在执行、阻塞、失败、未交付或未执行验证，必须直接回答“没有全部完成”并列出实际状态。
12. 无法取得事实或证据时必须说“无法确认”，不得猜测、把待执行描述为正在执行、复述未经核验的旧结论，或用“基本完成”“后续增强”掩盖未完成工作。
