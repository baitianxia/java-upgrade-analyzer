# 待优化项

本文只记录尚未完成且仍适用于当前 binary-first 架构的工作。历史实现问题不在现行 TODO 中保留。

状态快照日期：2026-08-10。当前共 2 项：待执行 1，待观测 1，阻塞 0，失败 0。

## 评估部分分析是否应恢复计算范围裁剪

- 优先级：待实际数据评估。执行状态：待观测。验证状态：当前行为已确认，方案待讨论。
- 当前事实：binary-first 流程在 Step4 中已经对全量变化依赖完成 runtime reconciliation、变化裁决、路径分析和独立 Oracle；Step5 的全量/部分选择目前只过滤发布与复核范围。因此部分分析仍能限制报告和结论的适用范围，但通常不能像 main 分支旧流程一样显著减少核心计算耗时。这与 README 中“部分分析可以降低耗时”的用户预期存在差距。
- 本轮决定：暂不调整 Step4/Step5 边界，也不预设必须恢复裁剪。先用实际项目观测全量与代表性部分范围的耗时，再决定保留全量预计算还是恢复真正的计算范围裁剪。
- 观测要求：同一实际项目固定相同 commit、制品、JDK、源码选择和依赖身份；分别记录全量与部分范围的冷/热运行，至少拆分 `artifact_fact_build_and_local_diff`、`target_independent_runtime_reconciliation`、`decision_and_projection_freeze`、`binary_trace`、`independent_validation`、Step5 发布及端到端耗时，并记录输入依赖数、变化 API 数、路径结果数、峰值内存和缓存命中状态。需要 main 分支参考时必须使用同一输入，且注明阶段职责不同。
- 正确性边界：任何候选方案都不能通过少扫依赖/API、关闭独立 Oracle、丢弃框架入口或复用旧引擎来换时间；部分运行中已选依赖的结果必须与同输入全量运行的对应投影一致，未选范围必须继续明确记录。
- 讨论出口：实际项目数据形成后，明确记录“耗时差异是否具有实际价值、节省来自哪个阶段、对准确性/内存/交互的影响”，再决定不改、只优化 Oracle/缓存，或把所选范围前移到路径与验证阶段。未形成该决策前不实施架构调整。

## 为归档安全校验建立显式策略等级

- 优先级：P2。执行状态：待执行。验证状态：问题已确认，改造尚未实施。
- 当前事实：生产调用点包括 Step1/gate 的 `require_safe_archive`，以及 binary artifact diff 和独立 Oracle 的 `inspect_archive`。binary artifact diff 已从版本化 support manifest 读取限制；其他调用仍以参数或默认值表达策略。
- 风险：内部生成归档、Step1 留存制品、完整 runtime 制品和潜在不可信外部归档的风险与性能预算不同；如果调用者未显式选择策略，新入口可能沿用不合适的默认限制。
- 待办：建立集中、不可变的命名策略，至少区分内部生成归档、Step1 浅扫描、完整 runtime 扫描和不可信外部归档；所有生产调用点显式选择策略。
- 验收：策略身份进入相关 cache/coverage；调整一种策略不改变其他策略；高膨胀率、超限总量、过多 entry、过深嵌套、重复路径、正常大型 JAR 均有正反例和资源预算验证。

历史审计见 [`docs/archive/2026-07-22-historical-zip-audit.md`](docs/archive/2026-07-22-historical-zip-audit.md)。
