# 待优化项

本文只记录尚未完成或尚未取得充分验证证据的工作。确认完成的项目直接删除，不在待办文档中保留历史设计正文；历史决策应进入 `docs/archive/` 或 Git 记录。

状态快照日期：2026-07-21。完整原则和移交上下文见
[`docs/developer/handoff.md`](docs/developer/handoff.md)。

当前共 12 项：已完成 0、正在执行 0、待执行 12、阻塞 0、失败 0。
所有条目的交付验证状态均为未验证。优先级含义：P0 为可能影响结论正确性或发布判断，P1 为性能、容量或测试门禁，P2 为维护与移交治理。

## 性能专项建议

性能优化集中在第 6～8 项，并受第 3、9、10 项的基线与结果等价门禁约束。当前最值得优先处理的不是盲目增加线程，而是减少重复扫描、重复解析和外部进程启动，同时把完整进程树的内存纳入度量。

### 建议实施顺序

1. **先补齐可观测性和基准。** 固定 1×/2×/4× 的源码、API、JAR 和类规模，记录总耗时、阶段耗时、单 API P50/P95/P99、完整进程树峰值 RSS、外部命令次数、缓存命中率、图节点/边数量及最终结果指纹。现有内存指标只统计 Python 主进程，暂时不能准确定位 OOM 风险。
2. **消除 `O(API × method)` 的重复源码扫描。** 建图时一次性生成类型、成员、声明类型和源码 token 到调用点的倒排索引；查询只访问索引，不再为每个目标遍历全部业务方法。方法正文缓存改成有上限的 LRU 或在阶段结束时释放，避免所有正文长期常驻。
3. **让多个 API 共享追踪计算。** 按依赖坐标和目标 owner 分组，采用多源反向遍历；对相同前驱子图、签名规范化和候选集合做 memoization。可以保留最终串行排序来保证确定性，但不应重复计算相同子图。
4. **减少 `javap/jdeps` 进程启动。** 优先用进程内 classfile 解析器一次读取常量池、descriptor、调用边、字段和继承信息；无法替代的命令按 JAR 批处理，并使用有界并发和背压。Commons Text 基线中 152 个类产生 219 次 `javap`，存在明确的压缩空间。
5. **建立可安全失效的持久事实缓存。** 缓存键包含制品 SHA-256、源码树 SHA、工具版本、解析 schema 和参数；缓存 classfile 事实、AST 摘要和符号索引，不直接缓存未经身份校验的最终结论。冷热缓存分别基准，制品或工具变化时自动失效。
6. **压缩图与证据的内存表示。** 对坐标、文件、FQCN、签名使用字符串驻留或整数 ID；邻接表避免在多个索引中复制完整 edge dict；路径证据按需物化，报告流式写出。目标是减少常驻对象，不删除边、不缩短分析范围。
7. **最后再增加并行度。** 仅并行互不依赖的 JAR/模块解析，并根据进程树 RSS、CPU 和文件描述符动态限制 worker；API 追踪若并行，必须保证缓存线程安全、结果稳定排序和相同输入字节级可重复。

### 性能验收红线

- 优化前后 changed API、制品、类、物理边、Oracle 闭集、五态结论、`path_details` 和 `alerts.csv` 结果指纹必须一致；不能通过减少扫描范围、降低深度或隐藏不确定性换速度。
- 1×/2×/4× 规模同时比较总耗时、P95 单 API 耗时、完整进程树峰值 RSS 和外部进程数；至少证明增长趋势改善，不能只展示单个小 fixture 的平均值。
- 分别报告冷启动、热缓存、缓存失效和低内存压力结果；命中 edge/class/depth 上限时必须失败关闭并进入质量信号，不能把截断当成成功优化。
- 建议以 Pig 和 RuoYi 作为容量压力样本，以 Commons Text 作为快速外部进程样本，再使用完整真实项目 guard 验证结果等价性。

## 1. 对齐历史进度记录、架构文档与当前代码

- 优先级：P1。执行状态：待执行。验证状态：未验证。
- 已核实事实：本机忽略文件 `.superpowers/sdd/progress.md` 仍以 `f376270` 为基线并写着
  `Task 4: in progress`；它不受 Git 跟踪，不能作为正式状态源。
- 已核实事实：`docs/developer/architecture.md` 多处仍称 Step5 为“四态”，但当前正式结果已经包含
  `reachable`、`not_impacted`、`uncertain`、`not_found_in_static_analysis`、`not_analyzed` 五态。
- 已核实事实：能力审计、质量和架构文档的部分结论早于当前证据模型、Oracle、产物身份和远程 ref 改动。
- 待办：逐条以代码、测试和提交记录核对旧 progress；统一五态、真实项目矩阵、质量门禁和当前能力边界的描述；仍未完成的工作只保留在本文件。
- 验收：不存在相互冲突的“进行中/已完成”、四态/五态或守护矩阵描述；每个状态陈述都能指向当前 commit 上的可复现证据。

## 2. 固化 Python、依赖和外部工具运行契约

- 优先级：P0。执行状态：待执行。验证状态：未验证。
- 已核实事实：仓库没有 `pyproject.toml`、requirements 或 lock 文件；CI 固定 Python 3.12、
  `tree-sitter==0.25.2`、`tree-sitter-java==0.23.5`，运行时自动安装却使用未固定版本。
- 已核实事实：当前本机 Python 3.14.6 上，tree-sitter 自动安装没有可用发行包，导致 quick 的
  `smoke_core` 失败；这说明“脚本可启动”不等于环境受支持。
- 已核实事实：`quality_gate.py` 的工具预检只调用 `shutil.which`，不校验命令能否执行、版本是否兼容、Python 包能否导入以及 JDK/Maven 是否来自同一有效工具链。
- 待办：明确支持的 Python/JDK/Maven/平台矩阵；增加固定依赖清单及可复现安装入口；将运行时联网安装改成显式 bootstrap 或受控离线缓存；预检增加版本、执行和 import smoke。
- 验收：干净环境可按一条文档化命令安装；受支持矩阵逐项通过，未支持版本在分析前给出明确错误；CI 与本地解析器版本完全一致且不依赖隐式网络副作用。

## 3. 建立当前 HEAD 的新鲜质量基线

- 优先级：P0。执行状态：待执行，依赖第 2、12 项。验证状态：未验证。
- 已核实事实：在 `d064f61` 上执行 quick 时，语法检查、Oracle 独立性、核心确定性、核心准确性基准和 21 项核心语义测试通过；`smoke_core` 因 Python 3.14/tree-sitter 环境不兼容失败，整轮约 31.9 秒。
- 无法确认：当前 HEAD 是否通过 Step5、完整 release 和最新真实项目守护矩阵；历史提交上的通过记录不能替代当前证据。
- 待办：环境收敛后依次运行 quick、Step5、Smoke、release，并用 `--include-real --real-case guard` 明确加入完整守护矩阵；保存 JSON、质量信号审计、复盘和 capability closure。
- 验收：结果绑定 commit、OS、Python/JDK/Maven、依赖锁、profile 和真实项目集合；没有失败、未解释跳过、fixture debt、阻塞信号或范围缩水。

## 4. 扩展类型语义与长链路覆盖，降低保守降级比例

- 优先级：P0。执行状态：待执行。验证状态：未验证。
- 已核实事实：类型直接使用目前主要依赖声明类型和少量文本模式（`new`、`.class`、`instanceof`、强转、FQCN）；对静态限定调用、注解、泛型参数、继承/实现、throws、方法引用等语法的完整覆盖尚无闭集证明。
- 已核实事实：默认 `max_total_cost=5`，高置信边约消耗 1、普通边约消耗 2，低置信边会停止；长依赖链或框架链会更早进入 `uncertain/not_analyzed`。这是失败关闭，不会直接证明“无影响”，但会降低可确认召回率。
- 待办：以 AST/字节码符号索引替代类型文本模式；为类型使用语法建立正例、同名负例和重载/泛型边界矩阵；对精确高置信边采用渐进加深或按图规模自适应预算，并记录被深度截断的目标和候选数。
- 验收：独立 Oracle 覆盖各类类型语法和 1/2/5/10+ 跳链路；增加预算只增加或保持确定证据，不改变已确认路径；所有截断均显式进入覆盖详情和阻断语义。

## 5. 明确 Kotlin 与混合语言工程的能力边界

- 优先级：P0。执行状态：待执行。验证状态：未验证。
- 已核实事实：Java 使用 tree-sitter 主路径，Kotlin 进入增强正则并记录 `unsupported_language_kotlin`；源码收集接受 `.java`、`.kt`，但分析器虽认识 `.kts`，收集器并未纳入 `.kts`。
- 已核实事实：正则分析器用路径含 `/test/` 或类名含 `Test` 推断测试代码，生产类名含 `Test` 时存在误分类风险。
- 风险：Kotlin 的扩展函数、顶层函数、默认参数、伴生对象、属性访问、类型别名和协程生成结构无法获得与 Java 相同的语义保证；当前真实项目门禁不足以量化差距。
- 待办：选择并文档化策略：引入 Kotlin PSI/compiler frontend，或明确 Kotlin 为 partial capability 并强制保守结论；补齐 `.kts`、标准源集识别和 Java/Kotlin 双向调用 fixture。
- 验收：混合语言 Oracle 对源码与最终字节码闭集核验；生产/测试分类依据源集而非类名；不支持的 Kotlin 结构不得产生确定的 `not_impacted`。

## 6. 建立一次扫描、多目标复用的源码事实索引

- 优先级：P1。执行状态：待执行。验证状态：未验证。
- 已核实事实：类型和字段直接使用按目标遍历全部业务方法；每个唯一目标至少进行一次扫描，方法正文还会被懒加载后长期缓存。API 多、方法多时接近 `O(API × method)`，并放大常驻内存。
- 已核实事实：当前 API 逐个串行回溯，虽然共享部分图和缓存，但没有从所有目标反向批量传播或复用相同子图的完整结果。
- 待办：建图时一次性生成 FQCN/成员/声明类型/正文 token 到调用点的倒排索引；按坐标、owner 和目标 key 分组追踪；对共同前驱使用多源反向遍历或 memoized 子图结果。
- 验收：结果指纹与完整 `alerts.csv` 逐行等价；在 1×/2×/4× API 与源码规模上扫描次数近线性、同一正文不被重复解析；记录索引构建成本、命中率和单 API P50/P95/P99。

## 7. 减少 `javap/jdeps` 等外部进程开销并安全复用事实

- 优先级：P1。执行状态：待执行。验证状态：未验证。
- 已核实事实：真实项目基线显示外部工具调用可成为主要成本，例如 Commons Text 152 个类对应 219 次 `javap`，Pig 基线包含 89,609 个类、约 107.8 秒 class 解析阶段和约 1.4 GB 主进程峰值记录。
- 待办：优先使用进程内 classfile 解析批量提取常量池、调用边、字段和继承关系；必须调用外部工具时按 JAR 批处理、限制并发并复用输出；建立由制品 SHA-256、工具版本、解析 schema 和参数共同寻址的持久事实缓存。
- 验收：冷/热缓存均有基准；缓存污染、制品替换、工具升级和并发写入测试通过；外部进程数显著下降且 Oracle、边账本、类覆盖和故障注入结果不变。

## 8. 观测并约束完整进程树的时间与内存

- 优先级：P1。执行状态：待执行。验证状态：未验证。
- 已核实事实：`step5_memory_observer.py` 读取当前 Python 进程 RSS 和 `RUSAGE_SELF` 峰值，不包含 `javap`、`jdeps`、Maven、JApiCmp 等子进程，也没有汇总并发子进程峰值。
- 风险：现有 `peak_rss_mb` 不能代表用户机器上的真实峰值，可能低估 OOM 风险；只看总耗时也无法区分 CPU、I/O、进程启动和 GC 压力。
- 待办：采集进程树峰值 RSS、子进程 CPU/墙钟时间、调用次数、并发度和临时文件体积；为图、正文缓存、字节码事实和报告阶段分别建立高水位；增加内存预算与自适应并发/淘汰策略。
- 验收：Linux/macOS 口径有文档和单测；压力测试能稳定触发软告警/硬阻断而非 OOM；优化前后同时比较完整结果指纹、P95 耗时和进程树峰值。

## 9. 拆分超大模块并收紧异常处理

- 优先级：P1。执行状态：待执行。验证状态：未验证。
- 已核实事实：生产代码约 8.8 万行，`confidence_weighted_tracer.py` 约 1.1 万行，`real_project_regression.py` 约 8.8 千行，`run_step.py` 和 `s4_jar_compare.py` 均超过 6 千行；仓库存在大量宽泛 `except Exception`、直接子进程调用和线程池入口。
- 风险：证据模型、策略判断、I/O 和报告渲染耦合后，局部修改容易绕过正式状态机或吞掉根因；超大测试文件也使门禁选择与失败定位变慢。
- 待办：按“事实提取—身份解析—图存储—追踪策略—结论收敛—输出渲染”拆分稳定接口；统一带阶段、命令、超时、stderr 和 blocking 属性的错误类型；清理裸 `except` 和没有覆盖详情的降级。
- 验收：模块依赖单向、核心策略可在纯内存 fixture 中测试；任何外部工具失败均映射到明确 reason code/coverage；拆分前后契约和结果指纹一致。

## 10. 强化测试框架的覆盖率、性质测试和变异测试

- 优先级：P1。执行状态：待执行。验证状态：未验证。
- 已核实事实：测试量很大（约 6.6 万行、1,900+ 个测试方法），但没有统一的分支覆盖率配置/门槛；最大测试文件超过 2 万行，测试发现与 profile 选择主要靠手工列表。
- 已核实事实：生产变异登记目前只有少量预设变异，不能系统检验 owner、签名、坐标、证据身份、深度和失败关闭规则是否真正被测试杀死。
- 待办：建立按 capability family 的测试清单和自动发现；对规范化、重载匹配、图路径、去重、状态单调性使用 property-based/metamorphic tests；扩大 mutation testing，并把未杀死变异、分支覆盖缺口和 flaky/时长排行纳入门禁。
- 验收：核心结论模块有分支覆盖门槛而非只看行覆盖；每个线上缺陷都有广义正例、负例、边界例和至少一个可杀死变异；测试可分片且 quick 的范围由声明式标签生成。

## 11. 修正质量门禁决策语义并扩大 CI 守护范围

- 优先级：P0。执行状态：待执行。验证状态：未验证。
- 已核实事实：所有 profile 默认省略真实项目；只要已计划的本地任务通过，JSON 就输出 `decision=release_allowed`。当真实项目任务完全未加入计划时，`real_project_skipped` 仍可能为 0，无法区分“未执行”和“执行且无 infra skip”。
- 已核实事实：定时 release workflow 显式包含真实项目，但只运行 `commons-text`，而 `real_project_regression.py --case guard` 可选择多个带 fixture manifest 的守护项目。
- 待办：把结果拆成 `local_regression_status`、`real_project_scope/status`、`release_decision`；非 release profile 或真实项目省略时使用 `not_evaluated`，不得输出允许发布；在 PR、主干、定时任务之间明确 quick/Step5/完整 guard 的分层和平台矩阵。
- 验收：dry-run、成功、失败、显式跳过、任务未计划和 infra skip 均有契约测试；只有固定依赖、完整必需 profile、声明的真实项目范围和质量审计全部通过时才允许发布。

## 12. 确认历史压缩包的归属与去向

- 优先级：P2。执行状态：待执行。验证状态：未验证。
- 已核实事实：移交文档编写时曾记录两个旧版完整打包快照；当前 `main` 工作区干净，仓库目录内已不存在这两个 ZIP。
- 无法确认：它们是已按所有者意图清理、移动到交付位置，还是仅从当前目录移除。
- 待办：确认是否仍需通过 GitHub Release/制品库交付历史版本；不把过期完整源码快照混入当前源码提交。
- 验收：需要保留的制品有版本、SHA-256 和交付位置；不需要的内容有所有者确认；工作区和移交说明一致。
